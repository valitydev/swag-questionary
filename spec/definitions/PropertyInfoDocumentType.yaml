type: object
discriminator: documentType
required:
  - documentType
properties:
  documentType:
    type: string
    enum:
      - LeaseContract
      - SubleaseContract
      - CertificateOfOwnership
      - OtherPropertyInfoDocumentType
