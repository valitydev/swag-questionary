type: object
required:
  - id
  - ownerId
  - data
properties:
  id:
    description: id анкеты
    type: string
  ownerId:
    description: id владельца анкеты
    type: string
  data:
    $ref: '#/definitions/QuestionaryData'
